import {onMounted, ref} from "vue";


export default {
    install(Vue: any) {
        // 전역 변수
        // 전역변수 선언 예시
        // Vue.config.globalProperties.전역 변수명 = "변수내용"

        // 전역변수 사용 예시 (화면별)
        // const app = getCurrentInstance();
        // const 사용할 변수명 = ref(app?.appContext.config.globalProperties.전역 변수명);
        
    }
}